import { extendTheme } from '@chakra-ui/react'

const theme = extendTheme({
  fonts: {
    prime: `'Heading Font Name', sans-serif`,
    second: `'Body Font Name', sans-serif`,
  },
})

export default theme